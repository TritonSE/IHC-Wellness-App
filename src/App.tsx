import * as React from 'react';
import Navigation from './View/Navigations/Navigation';

export default class App extends React.Component<{}, {}> {
  public render() {
    return (
      <Navigation></Navigation>
    );
  }
}
